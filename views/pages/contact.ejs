<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Contact Us - Community Garden</title>
  
  <!-- Stylesheets -->
  <link rel="stylesheet" href="/css/style.css" />
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  
  <!-- <script src="app.js"></script> -->
  
  <!-- Custom Garden Vibe Styles -->
  <style>
    body {
      background-color: #f0f8f4;
      font-family: 'Segoe UI', sans-serif;
      color: #333;
    }

    main {
      max-width: 700px;
      margin: 3rem auto;
      background: #ffffff;
      border-radius: 16px;
      padding: 2.5rem 3rem;
      box-shadow: 0 8px 20px rgba(0, 100, 64, 0.1);
    }

    h1 {
      text-align: center;
      color: #487531;
      margin-bottom: 1.5rem;
      font-weight: 700;
    }

    form input,
    form textarea {
      width: 100%;
      padding: 12px 16px;
      margin-bottom: 1rem;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 1rem;
    }

    form textarea {
      min-height: 140px;
      resize: vertical;
    }

    form button {
      background-color: #487531;
      color: white;
      padding: 12px 24px;
      border: none;
      border-radius: 8px;
      font-weight: bold;
      font-size: 1rem;
      transition: background-color 0.3s;
    }

    form button:hover {
      background-color: #487531;
    }

   
    main::before {
      content: '';
      position: absolute;
      top: 10px;
      right: 10px;
      background-size: 60px;
      width: 60px;
      height: 60px;
      opacity: 0.08;
    }
  </style>
</head>
<body>

  <%- include('../partials/header') %>

  <main>
    <h1>Contact Us</h1>
    <form method="POST" action="/api/messages">
      <input type="text" name="name" placeholder="Your Name" required />
      <input type="email" name="email" placeholder="Your Email" required />
      <textarea name="message" placeholder="Your Message" required></textarea>
      <div class="text-center">
        <button type="submit">Send Message</button>
      </div>
    </form>
  </main>

  <%- include('../partials/footer') %>
  <script src="appFrontEnd.js"></script>
</body>
</html>

